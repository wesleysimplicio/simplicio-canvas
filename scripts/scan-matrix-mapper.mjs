import { execFileSync } from 'node:child_process'
import { mkdtemp, readFile, rm, writeFile } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { resolve } from 'node:path'

const args = process.argv.slice(2); const idsArg = args.find((arg) => arg.startsWith('--ids=')); const output = args.find((arg) => arg.startsWith('--output='))?.slice('--output='.length); const determinism = args.includes('--determinism'); const timeoutSeconds = Number(args.find((arg) => arg.startsWith('--timeout-seconds='))?.slice('--timeout-seconds='.length) ?? 120); const timeout = Number.isFinite(timeoutSeconds) && timeoutSeconds > 0 ? timeoutSeconds : 120; const matrix = JSON.parse(await readFile(resolve(new URL('.', import.meta.url).pathname, '../fixtures/compatibility-matrix.json'), 'utf8')); const selected = idsArg ? matrix.repositories.filter((entry) => idsArg.slice(6).split(',').includes(entry.id)) : matrix.repositories.filter((entry) => entry.status !== 'planned' && entry.size !== 'large').slice(0, 3)
const report = { schema: 'simplicio-canvas/compatibility-mapper-scan/v1', generatedAt: new Date().toISOString(), mode: 'temporary-shallow-clone', sourcePolicy: 'metadata-only', determinismRequested: determinism, memoryMeasurement: 'parent-process-rss-delta', repositories: [] }
for (const entry of selected) {
  const fixture = await mkdtemp(`${tmpdir()}/simplicio-canvas-repo-`); const started = Date.now(); const memoryBefore = process.memoryUsage().rss; let result
  try {
    const root = resolve(fixture, entry.repository.split('/').at(-1)); execFileSync('git', ['clone', '--depth', '1', '--filter=blob:none', '--no-tags', `https://github.com/${entry.repository}.git`, root], { stdio: 'pipe', timeout: Math.max(120000, timeout * 1000), maxBuffer: 16 * 1024 * 1024 }); const scan = () => { execFileSync('simplicio-mapper', ['scan', root, '--sync', '--json', '--timeout', String(timeout)], { stdio: 'pipe', timeout: (timeout + 30) * 1000, maxBuffer: 16 * 1024 * 1024 }); return JSON.parse(execFileSync('simplicio-mapper', ['status', root, '--json', '--await', '--timeout', String(timeout)], { encoding: 'utf8', timeout: (timeout + 30) * 1000 })) }; const status = scan(); const repeated = determinism ? scan() : undefined; const version = execFileSync('simplicio-mapper', ['--version'], { encoding: 'utf8' }).trim(); const counts = { files: status.counts?.files ?? 0, layers: status.counts?.layers ?? 0, modules: status.counts?.modules ?? 0, symbols: status.counts?.symbols ?? 0, relationships: status.counts?.relationships ?? 0 }; result = { id: entry.id, repository: entry.repository, status: 'scanned', mapperVersion: version, timeoutSeconds: timeout, durationMs: Date.now() - started, phase: status.phase ?? 'complete', counts, determinism: determinism ? JSON.stringify(counts) === JSON.stringify({ files: repeated?.counts?.files ?? 0, layers: repeated?.counts?.layers ?? 0, modules: repeated?.counts?.modules ?? 0, symbols: repeated?.counts?.symbols ?? 0, relationships: repeated?.counts?.relationships ?? 0 }) : 'not-requested', memory: { rssBeforeBytes: memoryBefore, rssAfterBytes: process.memoryUsage().rss, rssDeltaBytes: process.memoryUsage().rss - memoryBefore }, diagnostics: { changedFiles: status.counts?.changed_files ?? 0, artifactsPresent: Boolean(status.artifacts_present), cacheEntries: status.cache?.entries ?? 0 } }
  } catch (error) { result = { id: entry.id, repository: entry.repository, status: 'unavailable', durationMs: Date.now() - started, reason: error instanceof Error ? error.message.slice(0, 180) : 'scan failed' } }
  finally { await rm(fixture, { recursive: true, force: true }) }
  report.repositories.push(result)
}
if (output) await writeFile(resolve(output), JSON.stringify(report, null, 2) + '\n'); console.log(JSON.stringify(report, null, 2))
